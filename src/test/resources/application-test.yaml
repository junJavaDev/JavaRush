# TODO 4/ add test profile to run app with h2 db in tests
spring.cache.type: none
spring:
  sql:
    init:
      mode: always
  liquibase:
    change-log: db/changelog/init-schema.sql
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;NON_KEYWORDS=VALUE;INIT=SET REFERENTIAL_INTEGRITY FALSE
    username: sa
    password: